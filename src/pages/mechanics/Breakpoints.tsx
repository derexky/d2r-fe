import { useState } from 'react';

type Row = (string | number)[];

// Faster Cast Rate — frame, 法蠻, 死聖, 德狼, 刺客, 德熊, 德魯依, 亞馬遜, A3傭兵, 法閃電, 塔格澳
const FCR_COLS = ['框', '法師/蠻族', '死靈/聖騎', '德(狼)', '刺客', '德(熊)', '德魯依', '亞馬遜', 'A3傭兵', '法師閃電', '塔格澳'];
const FCR: Row[] = [
  [7,  200,'','','','','','','','',''],
  [8,  105,'','','','','','','','',''],
  [9,   63,125,157,174,330,'','','','',''],
  [10,  37, 75, 95,102,163,163,'','','',''],
  [11,  20, 48, 60, 65, 99, 99,152,140,194,''],
  [12,   9, 30, 40, 42, 63, 68, 99, 90,117,''],
  [13,   0, 18, 26, 27, 40, 45, 68, 60, 78,180],
  [14,  '', 9, 14, 16, 26, 30, 48, 40, 52,120],
  [15,  '', 0,  6,  8, 15, 19, 32, 30, 35, 86],
  [16,  '','', 0,  0,  7, 10, 22, 15, 23, 65],
  [17,  '','','','', 0,  4, 14, 10, 15, 48],
  [18,  '','','','','', 0,  7,  0,  7, 35],
  [19,  '','','','','','', 0, '', 0, 24],
  [20,  '','','','','','','','','',18],
  [21,  '','','','','','','','','',11],
  [22,  '','','','','','','','','', 6],
  [23,  '','','','','','','','','', 0],
];

// Faster Block Rate — 亞單, 亞他, 蠻, 刺矛/杖, 刺他, 德全, 德人, 德熊, 德狼, 法, 死
const FBR_COLS_CLASS = ['框', '亞馬遜', '', '蠻族', '刺客', '', '德魯依', '', '', '', '法師', '死靈'];
const FBR_COLS_WPN   = ['',   '單手', '其他', '全', '矛/杖', '其他', '全', '人形', '熊', '狼', '全', '全'];
const FBR: Row[] = [
  [17,  0,'','','','','','','','','',''],
  [16,  4,'','','','','','','','','',''],
  [15,  6,'','','','','','','','','',''],
  [14, 11,'','','','','','','','','',''],
  [13, 15,'','', 0,'','','','','','',''],
  [12, 23,'','', 3,'','', 0,'','','',''],
  [11, 29,'','', 7,'','', 0, 5,'','', 0],
  [10, 40,'','',13,'','', 6,10,'','', 6],
  [ 9, 56,'','',20, 0,'',13,16, 0, 0,13],
  [ 8, 80,'','',32, 7,'',20,27, 7, 7,20],
  [ 7,120,'', 0,48,15,'',32,40,15,15,32],
  [ 6,200,'', 9,75,27,'',52,65,27,27,52],
  [ 5,480, 0,20,129,48, 0,86,109,48,48,86],
  [ 4,'',13,42,280,86,13,174,223,86,86,174],
  [ 3,'',32,86,'','',32,600,'',200,200,600],
  [ 2,'',86,280,'','',86,'','','','',''],
  [ 1,'',600,'','','',600,'','','','',''],
];

// Faster Hit Recovery — 德狼, 德熊, 德搖, 德其, 死, 聖矛/杖, 聖其, 刺, 蠻, A5傭, 亞, 塔格澳, 法, A1傭, A2傭, A3傭
const FHR_COLS_CLASS = ['框', '德魯依', '', '', '', '死靈', '聖騎', '', '刺客', '蠻族', 'A5傭', '亞馬遜', '塔格澳', '法師', 'A1傭', 'A2傭', 'A3傭'];
const FHR_COLS_WPN   = ['',   '狼', '熊', '搖', '其', '全', '矛/杖', '其', '全', '全', '全', '全', '全', '全', '全', '全', '全'];
const FHR: Row[] = [
  [ 2,280,'','','','','','','','','','','','','','',''],
  [ 3, 86,'','','','','',200,600,'','',600,'','','',''],
  [ 4, 42,360,456,377,280, 86,174,'','',168,'','','','',''],
  [ 5, 20,152,174,152,120, 48, 86,208,280, 86,280,600,'','',''],
  [ 6,  9, 86, 99, 86, 75, 27, 52,117,142, 52,142,232,'','',''],
  [ 7,  0, 54, 63, 56, 48, 15, 32, 72, 86, 32, 86,133,'','',''],
  [ 8, '', 37, 42, 39, 32,  7, 20, 48, 60, 20, 60, 86,'','',''],
  [ 9, '', 24, 29, 26, 20,  0, 13, 34, 42, 11, 42, 63,'','',''],
  [10, '', 16, 19, 16, 13, '','','','',  6, 24, 30,  6, 30, 46],
  [11, '', 10, 13, 10,  7, '','','','',  0, 16, 20,  0, 20, 32],
  [12, '',  5,  7,  5,  3, '','','','','', 10, 14,'', 14, 24],
  [13, '',  0,  3,  0,  0, '','','','','',  6,  9,'',  9, 18],
  [14, '','', 0,'','','','','','','','',  2,  5,'',  5, 13],
  [15, '','','','','','','','','','',  0,  0,'',  0,  8],
  [16, '','','','','','','','','','','','','','','',  5],
  [17, '','','','','','','','','','','','','','','',  0],
];

type TableProps = { cols1: string[]; cols2?: string[]; data: Row[]; };

function BpTable({ cols1, cols2, data }: TableProps) {
  return (
    <div className="overflow-x-auto">
      <table className="text-xs border-collapse" style={{ borderColor: '#222' }}>
        <thead>
          <tr>
            {cols1.map((h, i) => (
              <th
                key={i}
                className="px-2 py-1 text-center border"
                style={{
                  borderColor: '#2a2a2a',
                  color: h === '框' ? '#888' : '#c0a030',
                  background: '#111',
                  minWidth: i === 0 ? 32 : 52,
                }}
              >
                {h}
              </th>
            ))}
          </tr>
          {cols2 && (
            <tr>
              {cols2.map((h, i) => (
                <th
                  key={i}
                  className="px-2 py-1 text-center border"
                  style={{ borderColor: '#2a2a2a', color: '#666', background: '#0d0d0d' }}
                >
                  {h}
                </th>
              ))}
            </tr>
          )}
        </thead>
        <tbody>
          {data.map((row, ri) => {
            const frame = row[0] as number;
            return (
              <tr key={ri} style={{ background: ri % 2 === 0 ? '#0a0a0a' : '#111' }}>
                {row.map((cell, ci) => {
                  const isEmpty = cell === '';
                  const val = isEmpty ? '' : String(cell);
                  const isZero = val === '0';
                  return (
                    <td
                      key={ci}
                      className="px-2 py-0.5 text-center border"
                      style={{
                        borderColor: '#1a1a1a',
                        color: ci === 0 ? '#888' : isZero ? '#00ffcc' : isEmpty ? '#1a1a1a' : '#e0e0e0',
                        fontWeight: isZero ? 'bold' : undefined,
                      }}
                    >
                      {ci === 0 ? frame : val}
                    </td>
                  );
                })}
              </tr>
            );
          })}
        </tbody>
      </table>
    </div>
  );
}

const TABS = ['FCR 快速施展', 'FBR 快速格擋', 'FHR 快速受擊'];

export default function Breakpoints() {
  const [tab, setTab] = useState(0);

  return (
    <div className="text-sm">
      <h1 className="text-xl font-bold mb-1" style={{ color: '#00ffff' }}>
        攻速斷點表
      </h1>
      <p className="text-xs text-gray-500 mb-4">
        數值為達到該框數所需的 % 數；<span style={{ color: '#00ffcc' }}>0</span> = 基礎框數（不需額外加成）
      </p>

      <div className="flex gap-1 mb-4">
        {TABS.map((t, i) => (
          <button
            key={i}
            onClick={() => setTab(i)}
            className="px-3 py-1 text-xs border transition-colors"
            style={{
              borderColor: tab === i ? '#00cccc' : '#2a2a2a',
              color: tab === i ? '#00ffff' : '#888',
              background: tab === i ? '#001a1a' : 'transparent',
            }}
          >
            {t}
          </button>
        ))}
      </div>

      {tab === 0 && (
        <BpTable cols1={FCR_COLS} data={FCR} />
      )}
      {tab === 1 && (
        <BpTable cols1={FBR_COLS_CLASS} cols2={FBR_COLS_WPN} data={FBR} />
      )}
      {tab === 2 && (
        <BpTable cols1={FHR_COLS_CLASS} cols2={FHR_COLS_WPN} data={FHR} />
      )}
    </div>
  );
}
